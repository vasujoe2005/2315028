## Logging Middleware
Created a .env file and inputed the port ,bearer token and log api
Creater a file called server.js to connect the app to the server port 
created a router file to mange the routes
created a controller file which posts the log values of {server,level,package,message} to the log api that is initialised in the .env file and added the authorisation bearer token also in the .env file and checked the status
